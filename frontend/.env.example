# URL base del backend Django (API REST con JWT)
# Desarrollo local:
VITE_API_BASE_URL=http://127.0.0.1:8000
# Producción (Vercel): coloca la URL pública de tu backend desplegado, por ejemplo:
# VITE_API_BASE_URL=https://tu-backend.onrender.com
